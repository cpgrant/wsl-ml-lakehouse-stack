# Core
TZ=UTC
AWS_REGION=us-east-1

# MinIO (dev defaults)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=smoketest-bucket

# Airflow
AIRFLOW_UID=50000

# Jupyter
JUPYTER_TOKEN=letmein

# Compose (stable names even if folder is renamed)
COMPOSE_PROJECT_NAME=wsl-ml-stack
